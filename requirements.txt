# theright2read — refresh-pipeline dependencies. The public site ships
# zero Python at runtime; these deps run only when a maintainer regenerates
# assets/parliament_libraries.js from parliamentary records.
#
# Stage split:
# - commoner-probe owns acquisition/provenance into data/_parliament_libraries/.
# - sansad-semantic-crawler owns parse/analysis/export over that acquired corpus.

commoner-probe[http,pdf] @ git+https://github.com/CommonerLLP/commoner-probe.git@v0.3.0
sansad-semantic-crawler[http,pdf] @ git+https://github.com/CommonerLLP/sansad-semantic-crawler.git@294a77b9ef476ca5a3e582db57f495529095d977
